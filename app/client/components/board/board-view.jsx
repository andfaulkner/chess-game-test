// Third-party imports
import React from 'react';

/**
 * Board view UI component.
 * (React stateless functional component).
 */
export const BoardView = (props) => {
    return <div>Board view here</div>;
};
